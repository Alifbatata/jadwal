{
	"name": "@jadwal/sauvegarde",
	"version": "0.0.0",
	"private": true,
	"description": "La règle qui décide où part une archive de sauvegarde et sous quel nom. Isolée ici parce qu'elle se teste, et parce que le stockage ne sait rien d'autre que le préfixe.",
	"license": "MIT",
	"type": "module",
	"scripts": {
		"test": "vitest run --reporter=default --reporter=json --outputFile=.vitest-resultats.json"
	},
	"devDependencies": {
		"vitest": "catalog:"
	}
}
