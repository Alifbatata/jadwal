[Unit]
Description=jadwal — purges de rétention, une fois par nuit

[Timer]
# En UTC, pour la même raison que les autres : voir jadwal-prieres.timer et l ADR 0038.
OnCalendar=*-*-* 03:40:00 UTC
Persistent=true
RandomizedDelaySec=5m
AccuracySec=1min

[Install]
WantedBy=timers.target
