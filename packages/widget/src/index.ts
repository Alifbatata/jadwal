// Point d'entrée du fichier collé sur le site d'une organisation : enregistrer la balise, et rien
// d'autre.
import { register, TAG_NAME } from './element.js';

register();

export { TAG_NAME };
export { JadwalWidget, register } from './element.js';
