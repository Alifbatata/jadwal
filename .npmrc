# pnpm 12 ne lit dans ce fichier que l'authentification et les registres :
# save-exact et engine-strict sont déclarés dans pnpm-workspace.yaml (saveExact, engineStrict),
# où pnpm les applique. Les deux clés restent ici pour npm et les outils qui lisent .npmrc.
save-exact=true
engine-strict=true
